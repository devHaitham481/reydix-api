NODE_ENV=development
REDIS_HOST=localhost
REDIS_PORT=6379
JWT_SECRET=your-jwt-secret-key # Keep this for API Gateway logic
PORT=3000
# The API Gateway doesn't directly connect to fans-db via TypeORM based on your AppModule,
# but if it were to, it would need its own DB_HOST etc. based on the fans-db port.
# Based on your App module, it's not connecting to fans-db directly.
# If there's an implicit connection in some other module, ensure it uses localhost:5433
DB_HOST=reydix-db
DB_USER=reydix_user
DB_PASSWORD=Admin123
DB_NAME=reydix_db
